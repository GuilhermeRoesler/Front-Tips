import React from "react";

const Isolation = () => {
    return (
        <p>
            The <code>isolation</code> property is used to create a new stacking
            context without any other side effects.
        </p>
    );
};

export default Isolation;
